{
  "name": "electron-prj",
  "version": "0.0.0",
  "private": true,
  "description": "This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.",
  "homepage": "https://github.com/kittawat-thongpud/electron_react_template#readme",
  "bugs": {
    "url": "https://github.com/kittawat-thongpud/electron_react_template/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kittawat-thongpud/electron_react_template.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "npm run transpile:electron && npm-run-all --parallel dev:react dev:electron",
    "dev:react": "vite",
    "dev:electron": "npm run transpile:electron && cross-env NODE_ENV=development electron . --no-sandbox",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "transpile:electron": "tsc --project src/electron/tsconfig.json",
    "dist:mac": "npm run transpile:electron && npm run build && electron-builder --mac --arm64",
    "dist:win": "npm run transpile:electron && npm run build && electron-builder --win --x64",
    "dist:win86": "npm run transpile:electron && npm run build && electron-builder --win --x86",
    "dist:linux": "npm run transpile:electron && npm run build && electron-builder --linux --x64",
    "dist:linux86": "npm run transpile:electron && npm run build && electron-builder --linux --x86"
  },
  "dependencies": {
    "os-utils": "^0.0.14",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "@types/os-utils": "^0.0.4",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^4.4.1",
    "cross-env": "^7.0.3",
    "electron": "^36.3.2",
    "electron-builder": "^26.0.12",
    "eslint": "^9.25.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "globals": "^16.0.0",
    "npm-run-all": "^4.1.5",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.30.1",
    "vite": "^6.3.5"
  }
}
